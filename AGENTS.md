# Reader 项目协作规则

- 使用中文沟通，优先交付可验证结果。
- 先读 `README.md` 和 `docs/`，以代码与测试为最终事实来源。
- 保持单体 FastAPI + Jinja2 架构；没有明确收益时不要引入前端框架、数据库或额外服务。
- UI 变更同时检查桌面端和窄屏；目录与 AI 面板不得遮挡或挤坏正文。
- AI Key 只能存在于服务端环境变量；日志、测试、截图和文档不得出现真实密钥。
- AI 输出协议固定为 SSE：`token`、`done`、`error`；不得向前端暴露推理内容。
- 阅读分析自定义提示词不能替换服务端安全约束。
- 阅读笔记、划线和双页显示当前不在范围内；重新加入前先补产品说明和回归测试。
- 修改后至少运行 `uv run pytest -q`、`git diff --check`，前端脚本变更还要做 JavaScript 语法检查。
- 部署前阅读 `docs/operator-runbook.md`，先验证本地，再更新服务器；不得把临时容器改动当作唯一版本。
